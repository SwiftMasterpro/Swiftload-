import { Messages } from '@/components/features/Messages'
export const metadata = { title:'Messages', description:'Encrypted messaging between shippers and drivers' }
export default function MessagesPage() { return <Messages/> }
